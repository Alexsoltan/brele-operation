import "dotenv/config";
import { PrismaPg } from "@prisma/adapter-pg";
import { PrismaClient } from "../src/generated/prisma/client";

const adapter = new PrismaPg({
  connectionString: process.env.DATABASE_URL,
});

const prisma = new PrismaClient({ adapter });

const meetingTypes = [
  {
    name: "Синк",
    slug: "sync",
    description:
      "Регулярная рабочая встреча по статусу проекта, задачам, блокерам и следующим шагам.",
    isDefault: true,
    prompt: `Ты анализируешь регулярную рабочую встречу по проекту.

Главная задача — понять текущее состояние клиента, команды и проекта.

Особенно внимательно оцени:
- есть ли у клиента недовольство, тревога, усталость или потеря доверия;
- есть ли у команды ясный план и контроль над ситуацией;
- появились ли блокеры, задержки, неопределённость или риск срыва сроков;
- насколько понятны следующие шаги.

Верни только валидный JSON без markdown.`,
  },
  {
    name: "Демо",
    slug: "demo",
    description:
      "Встреча, на которой команда показывает клиенту результат работы, прототип, дизайн или функциональность.",
    isDefault: false,
    prompt: `Ты анализируешь демо-встречу с клиентом.

Главная задача — понять реакцию клиента на показанный результат.

Особенно внимательно оцени:
- принял ли клиент показанный результат;
- были ли возражения, сомнения, критика или разочарование;
- звучали ли признаки доверия или, наоборот, потери уверенности;
- насколько команда уверенно презентовала решение;
- есть ли риск переделок, изменения объёма или задержек.

Верни только валидный JSON без markdown.`,
  },
  {
    name: "Планирование",
    slug: "planning",
    description:
      "Встреча для планирования работ, сроков, ресурсов, приоритетов и договорённостей.",
    isDefault: false,
    prompt: `Ты анализируешь встречу по планированию проекта.

Главная задача — оценить ясность договорённостей, реалистичность сроков и риски по плану.

Особенно внимательно оцени:
- понятны ли цели и приоритеты;
- есть ли согласованность по срокам и объёму;
- есть ли риск перегруза команды;
- есть ли неясные зоны ответственности;
- проявляет ли клиент доверие к плану;
- звучат ли признаки будущего конфликта из-за сроков или ожиданий.

Верни только валидный JSON без markdown.`,
  },
  {
    name: "Приёмка",
    slug: "acceptance",
    description:
      "Встреча для согласования результата, финальных правок, принятия этапа или закрытия работ.",
    isDefault: false,
    prompt: `Ты анализируешь встречу по приёмке результата.

Главная задача — понять, принимает ли клиент результат и есть ли риск незакрытия этапа.

Особенно внимательно оцени:
- принял ли клиент результат;
- есть ли критичные замечания или только мелкие правки;
- есть ли риск отказа от приёмки;
- есть ли расхождение между ожиданиями клиента и фактическим результатом;
- насколько команда уверенно закрывает этап;
- понятны ли финальные действия после встречи.

Верни только валидный JSON без markdown.`,
  },
  {
    name: "Разбор рисков",
    slug: "risk",
    description:
      "Встреча, посвящённая проблемам, эскалациям, задержкам, конфликтам и управлению рисками.",
    isDefault: false,
    prompt: `Ты анализируешь встречу по рискам, проблемам или эскалации.

Главная задача — максимально строго оценить уровень риска проекта.

Особенно внимательно оцени:
- есть ли конфликт или напряжение;
- есть ли недовольство клиента;
- есть ли риск потери доверия;
- есть ли риск срыва сроков, бюджета или объёма;
- признаёт ли команда проблему;
- предлагает ли команда конкретный план выхода из ситуации;
- есть ли ответственные и следующие шаги.

Если клиент явно недоволен, clientMood не может быть good.
Если есть угроза срокам, доверию или результату, risk должен быть high.

Верни только валидный JSON без markdown.`,
  },
];

async function main() {
  await prisma.meeting.deleteMany();
  await prisma.project.deleteMany();
  await prisma.meetingType.deleteMany();
  await prisma.user.deleteMany();
  await prisma.workspace.deleteMany();

  const workspace = await prisma.workspace.create({
    data: {
      name: "Brele",
      slug: "brele",
    },
  });

  await prisma.user.create({
    data: {
      workspaceId: workspace.id,
      email: "admin@brele.local",
      name: "Admin",
      role: "ADMIN",
    },
  });

  await prisma.meetingType.createMany({
    data: meetingTypes.map((type) => ({
      workspaceId: workspace.id,
      ...type,
    })),
  });

  console.log("Seed completed");
  console.log(`Workspace: ${workspace.name}`);
  console.log("Admin: admin@brele.local");
  console.log(`Meeting types: ${meetingTypes.length}`);
}

main()
  .catch((error) => {
    console.error(error);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });